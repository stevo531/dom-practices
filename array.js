function first(arr, n = 1) {
    arr =[
        'a',
        'b',
        'c',
        'd',
        'e',
    ]
    //n = [0,1,2,3,4]
    
    let first(arr) = ['a'];
    let first(arr, 2) = ['a', 'b']
    let first(arr, 3) = ['a', 'b', 'c'];
    let first(arr, 0) = ['']
        
    //  n = arr[i]
    //let f = arr[0];
    // let l = arr[arr.length - 1];
 // 0  const arr = [1,
  // return arr;
  }
  // const elements = [1,2,3,4,5,6]
 console.log(first(arr))
 console.log(first(arr))
 console.log(first(arr))