// return sum of smallest integers

function ascending(numbers){
    numbers.sort((a,b) => a-b)
    return numbers[0] + numbers[1]
}
const numbers = [1,2,3,4,5];
const ascVar = ascending(numbers)
console.log(ascVar);

/* Write a function which takes in a string
and replace all characters with their corresponding numbers in their positions
in the alphabet and return an array of those numbers sorted in ascending order*/
function takein(text){
    const text = [1,2,3,4,5,5,-1]
    for(let i=0, i< text.length, i++){
        const texts = numbers.indexOf(char)
        if(arr === -1){
            console.log('oops')
         break   
        }
    }
}


const numbers = [1, 2, 3, 4, 5];

for (let i = 0; i < numbers.length; i++) {
  console.log(numbers[i]);
}


